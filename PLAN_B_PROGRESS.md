# Plan B Progress

Task-by-task tracker for Plan B (`in-progress/2026-04-21-sigil-effects.md`
in `boldfield/designs` while active; moves to `done/` when merged). Each
entry tracks: the task ID, current status, linked commits, and optional
notes on deviations. Deviations are logged separately in
`PLAN_B_DEVIATIONS.md` *before* the implementing commit.

Status values: `todo`, `in-progress`, `done`, `done-pending-ci`.

**Acceptance reminder (from plan's "Local verification strategy"):** a task
is not `done` until CI is green on both `x86_64-unknown-linux-gnu` and
`aarch64-apple-darwin`. Local pod checks are necessary but not sufficient.
`done-pending-ci` is the interim state for tasks whose local verification
is complete but whose CI run has not yet reported green.

## Stage 4.5 — Plan B scaffolding

- Task 4.5.1 — Create `PLAN_B_PROGRESS.md`
  - status: done
  - commits: [01e0e13]
  - notes: This file. Shipped in PR #14 (Stage 4.5 + A3 carryover + Task 47 parser scaffolding).
- Task 4.5.2 — Create empty `PLAN_B_DEVIATIONS.md`
  - status: done
  - commits: [01e0e13]
  - notes: Landed atomically with 4.5.1 (scaffolding is one unit) in PR #14.
- Task 4.5.3 — Plan B questions use `[PLAN-B]` prefix in `QUESTIONS.md`
  - status: done
  - commits: [01e0e13]
  - notes: Convention already documented in `QUESTIONS.md` header (added in Plan A2 Task 1.5.3). B entries follow the same convention; no header update required.
- Task 4.5.4 — Extend `.github/workflows/ci.yml` for Plan-B-specific invariants
  - status: done
  - commits: [01e0e13]
  - notes: `scripts/plan-b-invariants.sh` runs as a CI step; three named invariants (deep-recursion trampoline, multi-shot continuation, selective CPS) drive off specific `.sigil` example files and e2e test names. Each prints `[SKIP]` until the underlying example lands in Stage 5/6, at which point the step flips to running the test. CI logs surface the invariant scoreboard from day one so it cannot be dropped before acceptance.
- Task 4.5.5 — Create `sigil-abi` leaf crate; consolidate stackmap and cross-boundary constants
  - status: done
  - commits: [01e0e13]
  - notes: New `abi/` workspace member, `#![no_std]`, zero deps. Holds stackmap wire-format constants + `StackMapRecordV0` struct + tagged-Value bit masks + `TAG_INT_SHIFT`. `runtime::stackmap` and `runtime::value` `pub use`-re-export from it; the old `STACKMAP_*` pins in codegen.rs were removed. `sigil-header-constants` stays untouched (adjacent but distinct scope — the 8-byte object header). Pod-verify green.

## Plan A3 carryover

- Carryover — Full nested Maranget exhaustiveness
  - status: done
  - commits: [01e0e13]
  - notes: New recursive `match_witness(scrut_ty, &[&Pattern])` on `Tc` returns the first uncovered witness across user-type variants and their field patterns. `user_type_witness` now delegates to it. Primitive field rules unchanged (Bool → both literals required unless catchall; Int/Char/String/Byte/Fn → wildcard required). Nested witness formatting via `positional_witness_with_hole` and `record_witness_with_hole` — preserves declared field order and fills other slots with `_`. Six new tests cover: Some(true) missing `Some(false)`, Some(false) missing `Some(true)`, both-literals exhaustive, `Some(_)` catchall is exhaustive, `Holds(Leaf)` missing `Holds(Node(_, _, _))` on nested user types, `P { a: true, b: true }` missing `P { a: false, b: _ }`, and Int-field literal-only producing `Some(_)` witness (infinite-domain fallback). E0120 catalog long-form updated.
- Carryover — Suppress E0120 when an arm body fails type-checking
  - status: done
  - commits: [01e0e13]
  - notes: `check_match` tracks an `any_arm_erred` flag by snapshotting `self.errors.len()` before/after each arm's pattern + body check. If any arm added to the error list, the user-type E0120 emission is suppressed (per reviewer's narrowing — the primitive E0066 path runs unconditionally because primitive scrutinees rarely cascade the same way). Four tests pin the behavior: (1) suppression on arm-body type error (arithmetic on String), (2) suppression on arm-pattern E0117, (3) regression-guard that E0120 still fires on clean-but-non-exhaustive arms, (4) E0066 still fires on a non-exhaustive Bool match even when an arm body errs. E0120 catalog long-form updated.
- Carryover — Tagged-vs-raw Int ABI decision
  - status: done
  - commits: [01e0e13]
  - notes: Resolved to option (c) applied narrowly — raw `i64` internally in user-fn calls; tag at the C-ABI boundary only (main return). Stage 6 adds new tagging sites: continuations captured across handlers hold tagged Ints (heap-observable); arena-allocated `NextStep` records hold raw `i64` (arena is reset, not scanned). Both `ishl_imm` / `sshr_imm` sites in `codegen.rs` (main-return tag, C-main-shim untag) replaced their literal `1` with `i64::from(TAG_INT_SHIFT)`. Full rationale + audit table in PLAN_B_DEVIATIONS.md; cross-referenced `[PLAN-B] Tagged-vs-raw Int ABI` entry added to QUESTIONS.md closing the Plan A3 Forward-Implications paragraph.

## Stage 5 — Parametric polymorphism

- Task 47 — Parser: `[A, B]` generic params, explicit row vars `![IO | e]`
  - status: done
  - commits: [01e0e13]
  - notes: AST extensions: new `GenericParam` and `RowVar` types; `FnDecl` and `TypeDecl` gain `generic_params: Vec<GenericParam>`; `FnDecl` and `Expr::Lambda` gain `effect_row_var: Option<RowVar>`; `TypeExpr::Apply { name, args, span }` for generic application (`List[Int]`, `Map[String, List[Int]]`). New `TypeExpr::head_name()` and `span()` helpers keep most consumers unchanged. Parser: `parse_generic_params() -> Option<Vec<GenericParam>>` for `[A, B]` between name and `(`/`=` (errors propagate via `?` consistently with `parse_effect_row`); `parse_effect_row()` extracts effects + optional `| rowvar` body and replaces inline loops in `parse_fn_decl` and `parse_lambda_expr`; `parse_type` recognises `[T1, ...]` after a name and returns `TypeExpr::Apply`. **Semantic consumption deferred to Task 48** — typecheck rejects `TypeExpr::Apply` with E0124 and explicit row variables with E0125 so partial semantics cannot silently slip through; the new diagnostics live in the catalog with full long-form text and Task-48 fix examples. 12 parser tests + 9 typecheck tests cover the new shapes and the placeholder errors; all prior tests pass unchanged. Pod-verify green.
- Task 48 — Type checker: HM unification with row variables, closed rows
  - status: done
  - commits: [70756de]
  - notes: Added `Ty::Var(u32)` and changed `Ty::User(String)` → `Ty::User(String, Vec<Ty>)` so generic instantiations (`List[Int]`) round-trip through the inferred IR. New `FnSig.effect_row_var: Option<u32>` distinguishes closed rows from open rows. New `Subst` carries type-var → `Ty` and row-var → `Row` substitutions; `apply_ty` / `apply_row` resolve through it with cycle-safe walks. Unification: `unify_ty` does occurs-check (E0126), structural matching for `User` / `Fn` and recursive arg/param unification; `unify_row` enforces closed-row equality, closed-vs-open absorption, open-vs-open shared-tail merging, all with E0128 on mismatch and E0127 on row occurs failure. Call-site row check uses asymmetric `subsume_row` (callee's effects ⊆ caller's; callee's row var absorbs caller's leftovers) so the caller's declared row variable stays free for generalisation. Schemes (`type_vars`, `row_vars`, `body`) get instantiated at every call site of a top-level fn via `instantiate`; non-generic fns end up with empty bound-var lists so the legacy direct-call path stays zero-cost. **Pre-pass scheme seeding** registers a polymorphic `Scheme` in `fn_schemes` for every user fn before any body is checked, so forward and self references resolve through fresh-instantiation rather than a Unit-fallback `fn_env` entry — closing the source-order hole flagged in PR review. `check_fn` populates `current_generic_subst` from the fn's `[A, B]` list and `current_row_var_subst` from `![ ... | e]`, threading them through every `ty_from_type_expr_here` call inside the body. `check_lambda` now inherits the enclosing fn's `current_generic_subst` so a lambda inside `fn id[A]` can reference `A`; lambda body-vs-return also routes through `unify_ty`. Constructors of generic user types (`Cons(1, Nil)`) allocate fresh vars per declared generic param via `fresh_user_instance_with_subst`; field types resolve under the merged subst; arg-vs-field types unify, pinning the user-type args. **Pattern matching** likewise installs a per-pattern subst from the scrutinee `Ty::User(_, args)` zipped with the type's generic_params before resolving sub-pattern field types, so `match xs: List[Int] { Cons(h, _) => ... }` correctly types `h` as `Int`. E0124 / E0125 placeholder errors deleted from the catalog; replaced with E0126 (occurs), E0127 (row occurs), E0128 (row mismatch), E0129 (arity mismatch), E0131 (Apply on primitive / generic-param). Codegen-entry guard `contains_apply_or_generic_ref(&Program)` walks the AST for surface generic syntax and asserts at `emit_object` entry; the walker recurses into `Apply` args so a nested generic-param ref doesn't slip through. Four walker unit tests directly exercise the rejection / acceptance paths — closes the verification-debt entry "Codegen path for un-monomorphized generic params" fully (no Task 49 dependency). 33 new unit tests cover generic id, two-instantiation calls, generic compose-shape, generic ctor / variant / return type, arity mismatch (E0129), Apply on primitive (E0131), unknown head (E0112), open caller / closed callee, occurs check (direct unify_ty test for E0126), row-unifier direct tests for closed-vs-closed (E0128), closed-vs-open absorption, closed-vs-open missing-effect (E0128), open-vs-open shared-tail merge, row occurs (E0127), forward-reference between generic fns, pattern bind from generic ctor, Pair instantiation arg mismatch (E0044), self-recursive generic fn, lambda-internal type var, four codegen-walker rejection cases, plus all the prior Task 48 scenarios. All 281 typecheck tests pass + 4 codegen tests; pod-verify green.
- Task 49 — Monomorphization: reachability-bounded, typed IR preserved
  - status: done
  - commits: [858b4c2]
  - notes: New `compiler/src/monomorphize.rs` (~960 lines initial; ~1100 final after rounds 2–3) replaces the Plan A1 identity stub. Adds three-step pipeline: (1) instantiation capture during typecheck — new `pending_call_instantiations` and `pending_ctor_instantiations` on `Tc` track fresh-var ids per `Expr::Ident` use of a top-level fn (via `instantiate_with_vars`) and per ctor use of a generic user type (via `fresh_user_instance_with_subst_and_ids`); end-of-typecheck resolves them through `subst` to produce concrete `Vec<Ty>` per site, exposed as `CheckedProgram::call_site_instantiations` and `CheckedProgram::ctor_site_instantiations` keyed by use-site span; (2) post-elaborate worklist BFS rooted at `main` — `program_has_generics` early-returns for non-generic programs (every Plan A1/A2/A3 program), preserving zero overhead; otherwise `Monomorphizer::run` drains fn and type worklists to fixpoint, cloning each generic decl per (name, type-args) tuple; (3) AST rewrite pass — every cloned body has its `TypeExpr::Apply` and generic-param `TypeExpr::Named` resolved to concrete primitives or mangled user-type names, every fn-call callee Ident rewritten to `mangle_fn(name, args)`, every ctor site rewritten to `mangle_ctor(name, type_args)`. **Canonical mangling** pinned via PR #16 round-2 fixup (`994f083`): `$` separator within a single type-arg, `$$` between top-level args. Top-level fn → `f$$<canon(T1)>$$<canon(T2)>$$...`. Top-level type → `Foo$$<canon(T1)>$$...`. Ctor of generic type → `C$$<canon(T1)>$$...`. `canon(User(name, args))` = `name` for arity 0, else `name$<canon(a1)>$...`. The `$` character is lexer-rejected in surface syntax (precedent: A2's `$lambda_N` synthetic names) so post-mono identifiers cannot collide with user identifiers — closed the round-1 soundness hazard where `type List_Option[A]` instantiated at Int could silently collide with `List[Option[Int]]` in `type_seen`. Plan example pinned by tests: `list_map$$List$Option$Int$$List$Option$Int`. New E0132 ambiguous-polymorphism diagnostic at end-of-typecheck. canon_ty `Ty::Var` / `Ty::Fn` arms hardened to `unreachable!`. **Effect rows preserved** per Plan B v1: codegen-entry walker at `compiler/src/codegen.rs:128` no longer rejects `f.effect_row_var.is_some()` or `Expr::Lambda { effect_row_var: Some(_), .. }`. **Pattern-ctor rewrite** uses `match_scrut_tys` + a new `ctor_to_type` index built from the original TypeDecls; round-3 fixup (`7a76c43`) added per-sub-pattern field-type threading via `Monomorphizer::variant_field_types(owning, ctor, type_args)` — closes the regression where nested generic ctor patterns over `Option[List[Int]]` panicked at the Plan A1 fall-through `unreachable!`. **`CheckedProgram::fn_schemes`** now exposed publicly so monomorph can map fresh-var ids back to declared generic-param names. New `GenericInstantiation` type. 40 net new unit tests across rounds 1–3 cover: `canon_ty` rendering for all primitive and user variants, mangle_fn/mangle_type/mangle_ctor for empty / single / multi / nested arg cases including the plan's `list_map$$List$Option$Int$$List$Option$Int` example, Substitution::apply_to_ty resolving Var/User/Fn through subst, ty_to_type_expr round-trip, 9 end-to-end pipeline tests through `lex → parse → resolve → typecheck → elaborate → monomorphize`, `nested_generic_ctor_pattern_threads_inner_scrut_ty` (round-3 reproducer with outer `Some$$List$Int`/`None$$List$Int` and inner `Cons$$Int`/`Nil$$Int`), and `effect_row_var_preserved_on_clone_passes_codegen_walker`. 281 → 321 compiler lib tests across the rounds.
- Task 50 — Color inference: per-monomorph, SCC-aware, `--dump-color`
  - status: done
  - commits: [82e0f97]
  - notes: New `compiler/src/color.rs` replaces the Plan A1 always-Native stub. Per-monomorph local analysis: open effect row (`![IO | e]`) → CPS; non-`IO` effect in row → CPS; body (or nested lambda body) performs a non-`IO` effect → CPS; else native. Reasons are stable, machine-readable, and human-readable. **Iterative Tarjan SCC** over the call graph (no recursive frames; safe under monomorph counts that grow with typeclass dictionary specialization). **Reason text per-node names each fn's own most-proximate cause**: intrinsic-CPS members keep their specific local reason; bridge members (with outgoing edge to a CPS SCC in a different SCC) get `cps: transitively calls <callee> which is cps`; non-bridge members propagated through SCC membership get either `cps: in SCC with intrinsically-cps member <name>` or `cps: in SCC bridging to cps callee via <name>` depending on which peer caused the taint. **Edge insertion is precise**: drives off `CheckedProgram::call_site_instantiations` (typecheck records every Ident — call or value position — that resolved to a top-level fn under env-precedence rules), so a parameter or `let` binding shadowing a top-level fn name does NOT produce a spurious edge. **`--dump-color` CLI flag** lands in `cli.rs` + `main.rs` + new `pipeline::dump_color` helper: prints `<name> native|cps <reason>` per fn (one line per monomorph in program order) to stdout, no codegen. `-o <path>` is accepted under `--dump-color` (shell-history ergonomics) and the path is recorded so `main.rs` prints a stderr warning that no executable was produced; `--print-runtime-stats` conflicts and emits a usage error. **`pipeline::DumpColorError` enum** distinguishes ReadFailed from FrontEndErrors(usize). **20 color::tests** unit tests cover all classifications + SCC algorithm corner cases: open-row CPS, non-IO-row CPS, perform-non-IO CPS, caller-callee both native, mutual recursion native, mutual recursion with one CPS member tainting whole SCC, transitive CPS taint, unrelated cycle non-pessimization, lambda body perform tainting parent, **self-loop single-node SCC** (native + CPS variants), **let-bound-fn-value taints parent via outgoing edge**, **3-SCC chain transitive propagation**, **transitive-only SCC reason branch** (no intrinsic member, only bridge member), **parameter shadowing precision** (now discriminating — see review-fixup-2 below). 11 cli::tests (4 for --dump-color: default + human formats, -o-recorded-for-warning, conflict with --print-runtime-stats). 2 e2e tests exercise `sigil <input> --dump-color` end-to-end. Pod-verify green; full lib tests deferred to CI. Review-fixup-1 commit: addresses all six "must-fix" items from PR #17 review (iterative Tarjan, span-keyed edges, per-node reason proximity with two-variant SCC fallback, `Result<String, DumpColorError>`, `-o` warning, renamed `unused_param_warning_silenced`). Review-fixup-2 commit: addresses the non-blocking residual concern that `parameter_shadowing_top_level_fn_does_not_taint_caller` was non-discriminating under Stage 5 (typecheck rejects non-IO effects, so `dangerous` ended up Native and the test passed under either edge regime). Reviewer's option 3 applied — added `unique_span()` test helper (atomic-counter span generator) plus `synth_program_with_calls(items, calls)` test helper that takes an explicit calls map. Rewrote the test as a synthetic program: `dangerous` is intrinsically CPS (Raise effect), `unrelated` is Native, `caller(dangerous: Int)` body has both a param-ref `dangerous` (shadow Ident with unique span deliberately omitted from calls map) AND a real call to `unrelated()` (Ident span recorded in calls map). Asserts `caller` Native — discriminating: under the old name-only heuristic caller would acquire a spurious edge to dangerous (CPS) and falsely classify CPS, but the precise calls-map-driven edge logic excludes the shadow Ident.
- Task 51 — `examples/generic_map.sigil` + e2e
  - status: done
  - commits: [6305bcb]
  - notes: New `examples/generic_map.sigil` declares `type List[A] = | Nil | Cons(A, List[A])`, `fn map[A](xs: List[A]) -> List[A] ![]` (structure-preserving traversal — Sigil v1 has no `TypeExpr::Fn` surface syntax, so the design doc's canonical higher-order `map[A,B,e](xs, f) -> List[B]` is not expressible; this is the closest Plan B can express, semantically `map id`), and `fn length[A](xs: List[A]) -> Int ![]` (generic fold to a concrete scalar). `main` instantiates each generic at `Int` and `String` in the same scope, producing four monomorph clones (`map$$Int`, `map$$String`, `length$$Int`, `length$$String`) plus mangled ctor sites (`Nil$$Int`, `Cons$$Int`, `Nil$$String`, `Cons$$String`). Two e2e tests in `compiler/tests/e2e.rs`: `generic_map_example_prints_3_and_2` exercises the full lex → parse → typecheck → elaborate → monomorphize → color → codegen → run pipeline (the FIRST end-to-end test of user-authored generic syntax through codegen, per PR #17 reviewer's Stage 5 review note); `generic_map_dump_color_all_native` exercises Task 50's per-monomorph color inference, pinning each expected mangled name independently so a mangling-format slip on any single one (e.g. `map$$String`) lands on a directed assertion. `scripts/smoke.sh` and `scripts/reproducibility.sh` updated to include the new example so the per-host byte-stable-binary invariant covers the full generic pipeline.
- Task 52 — Validation prompts P16, P17
  - status: done
  - commits: [6305bcb]
  - notes: P16 — generic identity at Int and String; fully expressible in Plan B's surface (no `TypeExpr::Fn` required); oracle `"42\nsigil\n"`. P17 — generic compose applied across types; requires `TypeExpr::Fn` for the higher-order parameters (same dependency as P09 / P10), so deferred to whenever first-class function types ship; until then, graded only against "program compiles". P17's distinguishing feature versus P10 is `A != C` (the result type genuinely travels through composition rather than absorbing into the trivial endo-functor case). Bank now at 17/20; P18-P20 land with Stage 6 Task 61 (Raise-based parser, State-threaded counter, multi-shot Choose).

### Stage 5 review checkpoint

Pending — request human review of row-unification, let-generalization, color inference, and monomorphization naming determinism before Stage 6 begins.

## Stage 6 — Algebraic effects and handlers

- Task 53 — Parser: `effect Name[T] { ... }`, `resumes: many`, `handle expr with { ... }`
  - status: done
  - commits: [2ed6628]
  - notes: Added Stage-6 lexer keywords `effect`, `handle`, `with`; the attribute words `resumes` and `many` stay as plain identifiers and are matched contextually inside `parse_effect_decl` so user code that legitimately wants `let resumes = 5` is not regressed (4 lexer tests cover this). AST extensions: new `Item::Effect(Box<EffectDecl>)`; `EffectDecl { name, name_span, generic_params, resumes_many, ops, span }`; `EffectOp { name, name_span, params, return_type, span }`. New `Expr::Handle { body, return_arm: Option<Box<HandleReturnArm>>, op_arms: Vec<HandleOpArm>, span }` with supporting `HandleReturnArm` (single value binding + body) and `HandleOpArm { effect, effect_span, op, op_span, params: Vec<HandleArmParam>, k_name, k_span, body, span }`. Parser: `parse_effect_decl` consumes the optional generic-param list, the optional `resumes:many` attribute, and a non-empty op list with trailing-comma support; `parse_effect_op` handles `name : ( T1, ... ) -> R`; `parse_handle_expr` parses the body under `no_record_lits=true` (so `handle Foo with { ... }` doesn't try to read `Foo {` as a record literal), then `with { return(v) => ..., Effect.op(p1, ..., k) => ..., ... }` arms with at-least-one-op-arm + at-most-one-return-arm + mandatory trailing continuation `k` rules enforced at parse time. The arm grammar uses fully-qualified `Effect.op(...)` rather than the plan body's bare `op(...)` so a single `handle` can dispatch operations from more than one effect; the qualified form also mirrors `perform Effect.op(args)` for visual symmetry. Documented as Plan B Task 53 design choice in PLAN_B_DEVIATIONS.md (deviation entry added with this commit). Staged errors `E0133` (effect decl pending Task 54) and `E0134` (handle expression pending Task 54+55) added to the catalog with full long-form text + workaround examples; typecheck emits each at every encountered shape so a partial Plan B program cannot reach mono/CPS/codegen until Task 54 lifts the gate. The E0001 discipline sweep (`no_user_facing_error_uses_e0001`) gains two new programs covering the staged shapes. Downstream stubs added in elaborate (pass-through), closure_convert (capture-analysis recursion through arm bodies + arm bindings), monomorphize (substitution recursion + Item::Effect preservation through the BFS rebuilder), color (arm-body recursion in both `find_non_io_perform_in_expr` and `collect_calls_in_expr` plus the test-only `walk_expr_for_fn_idents`), and codegen (`unreachable!` in `lower_expr` + `type_of_expr`; defensive walker arm in `expr_uses_generic` and a hard-reject Item::Effect in `contains_apply_or_generic_ref`). Tests: 18 new parser unit tests covering all happy and error paths (effect decls with/without generics/resumes, multi-op decls with trailing comma, empty body / non-`many` rejection, `resumes` outside-effect-decl regression guard for the context-keyword choice, minimal handle, return-arm + multi-effect handle, k-binding-as-trailing-param, missing-with / empty-arms / no-k / duplicate-return error paths, body-no-record-lit regression). 5 new typecheck unit tests pin E0133 / E0134 emission and the cascade-friendly recursion behavior. 5 new lexer tests pin keyword recognition. Pod-verify clean.
- Task 54 — Type checker: row-polymorphic effect checking, handler typing, one-shot linearity (E0220)
  - status: done
  - commits: [f0b68b3]
  - notes: Effect declarations now register into a `Tc.effects: BTreeMap<String, EffectDecl>` registry built in the top-level pre-pass (and exposed on `CheckedProgram.effects` for Task 55's CPS transform). Duplicate effect names emit `E0136` against the second offender's name span; duplicate operation names within an effect emit `E0137` against the second op's name span. Both follow the Plan A3 type-pre-pass convention: first declaration wins, dedup happens before any body walk. **Per [DEVIATION Task 54]**, `E0133` and `E0134` stay live through Task 54 — the typechecker does the full handler-typing work internally, but the staged-feature gates remain so partial Plan B programs cannot reach the still-CPS-pending codegen path. Lifting both gates is Task 55's first job. **Internal handler typing**: replaced the `Expr::Handle` typecheck arm with a three-phase `check_handle` (1) **dispatch table**: per-arm registry lookup (E0138 unknown effect / E0139 unknown op), duplicate-arm detection (E0140), arm-arity check (E0141), and a per-handle effect-instantiation cache so cross-perform / cross-arm generic-parameter substitutions stay consistent within a single handle; (2) **body walk**: body row = caller-row ∪ discharged-effects (deduped, sorted), pushed against a `Tc.handler_scopes` stack so `perform Effect.op(...)` sites inside the body share the handle's substitution; (3) **arm walks + linearity**: each op-arm installs its user-param + `k` bindings into a snapshot/restore env scope (caller-row scope, not body-row — discharged effects are NOT in scope inside arm bodies), unifies the arm body's type with a fresh handler-overall var, then runs the one-shot linearity check on the body. Even arms whose registry lookup failed get `Ty::Unit`-fallback bindings + walked body so structural diagnostics inside the arm surface alongside the registry diagnostic — Task 53 prep item 10 closed. **`check_perform` registry dispatch**: non-IO `perform` sites now dispatch through `Tc.effects` (with the IO branch unchanged for Task 57). Param + return tys resolve under the effect's generic substitution; arg unification uses HM `unify_ty`. **One-shot linearity check** (`E0220`) implemented as a path-max syntactic counter `count_continuation_uses(expr, k_name)`: branching constructs (`if`, `match`, nested `handle` arm bodies) take the max across branches, sequential composition (block stmts, binary ops, call args) takes the sum, both saturate at 2 (the > 1 vs <= 1 distinction is what the linearity rule needs). References to `k` from inside a `lambda` body saturate to 2 immediately (conservative-capture rule documented in [DEVIATION Task 54]). Multi-shot arms (`effect E resumes: many`) skip the check. Lexical shadowing in patterns / nested handle arm `k_names` / `let` stmts suspends counting. **Codegen-gate alignment with Task 55** (Task 53 prep item 11): Both `Item::Effect → return true` short-circuit in `contains_apply_or_generic_ref` and the `Expr::Handle => unreachable!` arm in `lower_expr` / `type_of_expr` stay in place; E0133 + E0134 prevent any well-formed effect / handle program from reaching codegen, so the `unreachable!`s never fire. Task 55's first PR lifts both gates and replaces the `unreachable!`s with the CPS expansion. **API surface delta**: new `CheckedProgram::effects: BTreeMap<String, EffectDecl>` field. New catalog codes E0136 / E0137 / E0138 / E0139 / E0140 / E0141 / E0220 added with full long-form text + fix examples. Tests: 30 new typecheck unit tests covering registry diagnostics (E0136 / E0137 / E0138 / E0139 / E0140 / E0141), env-extension regression (Task 53 prep item 10: arm-param / k / return-arm bindings no longer fire spurious E0046), handler overall-type computation (with and without return arm; cross-arm body unification firing E0044 on mismatch), body-row extension (handle body can perform discharged effects; arm bodies cannot), `perform` via registry (typechecks under declared row, fires E0043 / E0044 on mismatch, E0042 on unknown effect even when listed in row), and the full E0220 matrix (zero / one use is fine; sequential and branch-then-extra usage fires; branches that each use `k` once independently are fine; lambda capture is rejected; multi-shot skips; shadowing suspends counting; direct unit tests on the counter helper for literals / saturation / branches / lambda capture). 376 → 410 compiler lib tests (+34 net: 30 new Task 54 tests, plus the catalog gains 7 entries which shows up in the lib total via the catalog's `seed_entries_are_unique_and_non_empty` discipline test). Pod-verify clean.
  - prep (closed in this commit):
    - **(10)** ✅ closed — `check_handle` extends the env with op-arm params + k continuation + return-arm binding before walking arm bodies. Tests `handle_arm_param_bindings_no_spurious_e0046` / `handle_arm_k_binding_no_spurious_e0046` / `handle_return_arm_v_binding_no_spurious_e0046` pin the regression.
    - **(11)** ✅ closed via the "preferred" option from PR #19's review: E0133 / E0134 stay live through Task 54 so well-formed effect/handle programs cannot reach the CPS-pending codegen path. The codegen-entry walker's `Item::Effect → return true` short-circuit and the `Expr::Handle => unreachable!` arms in `lower_expr` / `type_of_expr` remain unchanged; Task 55's PR will lift the gates and replace the `unreachable!`s with CPS expansion in a single focused change. [DEVIATION Task 54] entry on the staged-gate strategy spells out the alternative (extending the codegen walker to short-circuit `Expr::Handle` directly) and why we declined it.
- Task 55 — CPS transform on CPS-color monomorphs; arena-allocated `NextStep` records
  - status: done-pending-ci (foundation + Phase 2 + Phase 3a + Phase 3b + Phase 4a + Phase 4b + Phase 4c + Phase 4d MVP + **Phase 4e PR #26 (squash-merged at `2a3cb25`)** + **Phase 4e captures+ PR #27 (squash-merged at `3affced` on 2026-04-28)** all close Phase 4e + **Phase 4f PR #28 (squash-merged at `08d002a` on 2026-04-28)** closes multi-effect handles + **Phase 4g PR #29 (squash-merged at `a777748` on 2026-04-28)** closes return arms — Task 55 Phase 4 work complete; only Tasks 57–61 + Stage 6 review checkpoint remain in Plan B Stage 6.)
  - **Phase 4e in-flight progress on `plan-b-task-55-phase-4e`** (CI-validated through `1a8ea17`): foundation deviation entry (`a8b9dbd`) + 3 colorer regression tests (`5cc3a58`) + section-4 reframe (`d8a4771`) + e2e pinning test (`df251fc`) + hard-condition #2 fix (`eae3589`) + cps.rs design plan + accessors (`a756bd3`) + 5-item review-fix bundle (`06c3459`) + synthetic-continuations bypass + multi-member SCC test (`5514a67`) + **CpsProgram wrapper deletion** (`3c65054`; accessors moved to `ColoredProgram` directly) + `cps_signature` helper (`b1244b4`) + `is_simple_tail_perform_with_pure_args_body` classifier (`76c17ae`) + `UserFnAbi` enum + `compute_user_fn_abi` selector (`a2840b6`) + 5-item review-fix bundle (`cbe95fc`: arg-purity tightening + `UserFnAbi::Native`→`Sync` rename + Native-short-circuit test) + **CPS-ABI first slice** (`33f2231`: signature selection + body emission for arity-0 simple-tail-perform + native↔CPS interop wrapper + happy-path e2e) + 4-item review-fix bundle (`5a0459a`: D1 arity gate + D2 args_len convention + S1 offset constants + S3 coverage tests for multi-call / Bool / String returns) + **arity-N widening** (`1a8ea17`: D1 arity gates removed; CPS body emission unpacks user args from `args_ptr[0..N*8]` via Lowerer; caller wrapper packs N user args + trailing (k_closure, k_fn); 2 prior D1 regression tests inverted Sync→Cps; new arity-N e2e). 19 commits total. **CI green at `1a8ea17`** on all 4 lanes.
  - **Phase 4e PR #26 — squash-merged at `2a3cb25`.** All architectural foundation landed (CPS-color user-fn calling convention + native↔CPS interop wrappers + codegen-consumes-color per-fn ABI selection + colorer regression-pinning) + helper-side lambda-lifting first slices (ConstantDone constant-tail synth-cont at `b818fc3`; captures-free LetBindThenTail at `2a9958b`; captures-bearing LetBindThenTail at `a5ee4c6`) + hard condition #2 closed (both `discard_k_handler_does_not_abort_helper_phase_4e_pending` and `statement_form_non_io_perform_inside_handle_compiles_and_runs` test inversions) + FFI-ref dedup (`prepare_per_fn_refs` helper at `73c7e53`) + pattern-walker dedup + Char capture e2e + doc-comment precision (`adf0e23`) + cadence-pivot closeout-prep (`fe2e6b9`). 28 commits squash-merged. The deferred lifts (items 5/6/7) ship in the captures+ follow-up.
  - **Phase 4e captures+ PR #27** (`plan-b-task-55-phase-4e-captures` branch against `main` post-PR-#26-merge; **squash-merged at `3affced` on 2026-04-28**). Five-slice roadmap shipped per `[DEVIATION Task 55] Phase 4e captures+` in `PLAN_B_DEVIATIONS.md`:
    - **Slice A** (`d91b22c` + `b1d6845` fixup + `0dce45f` polish) — helper synth-cont signature shift to uniform trailing-pair read. `args_ptr=[arg, post_arm_k_closure, post_arm_k_fn]`, returns `Call(post_arm_k_*, [result])` instead of `Done(result)`. Tail-`k` arms emit `[arg, null, &identity]`. Invariant-preserving foundation refactor; identity intrinsic's args_len assertion relaxed to `{1, 3}`.
    - **Slice B** (`e5991a9` + `2e7e444` polish) — non-tail `k` use in arm bodies (item 5 of comprehensive entry). Parser extended to accept `Block` as primary expression. Walker rejection lifts for the let-then-pure-tail shape. Arm body's post-`k` rest lambda-lifted into a post-arm-k synth fn dispatched via the trailing-pair convention.
    - **Slice C** (`687da3a` lift + `5fbda51` debug + `113b7da` fix + `c898ebd` polish + `5bf9a73` polish-fixup + `9d2dbea` polish round 2) — multi-shot `k` via heap-reified continuation (item 6). Multi-let arm body shape `{ let r1 = k(arg1); let r2 = k(arg2); pure_tail }` for `resumes: many` effects. Two chained post-arm-k synth fns; `post_arm_k_1` captures `(k_closure, k_fn)` for the second `k(arg2)` invocation; `post_arm_k_2` captures `r1` for tail combination. Choose-style e2e test exercises end-to-end. Latent Slice B body_ty ≠ tail_ty bug surfaced + retroactively fixed.
    - **Slice D** (`f45a9be`) — surrounding-lambda closure captures into arm bodies (item 7). Walker's `Expr::ClosureEnvLoad` rejection lifted. `ArmCapture::lambda_source` field populated by codegen pre-pass scanning the post-closure_convert arm body for matching ClosureEnvLoad nodes; `alloc_arm_closure_record` sources values via `lower_closure_env_load` against the lifted lambda's `closure_ptr` when set. Phase 4d MVP `#[ignore]`'d test `arm_inside_lambda_captures_outer_via_closure_env_load_is_rejected_at_codegen_phase_4e_pending` inverted to positive (asserts stdout `7`).
    - **Closeout (this commit)** — walker's Phase-4e-pointing diagnostics rewritten to reflect what's now closed; README "Verification limits" final cleanup with Phase 4f/4g framing; PROGRESS Phase 4e status flipped to `done-pending-ci`.
  - **Phase enumeration renumbered as of Phase 4d** (see `[DEVIATION Task 55] Phase 4d MVP scope` entry in `PLAN_B_DEVIATIONS.md`): Phase 4e is **NEW** (colorer's handler-discharge refinement + non-tail k + discard-k correctness — closes the algebraic-semantics gap that gates Stage 9 spec validation; the PR #18 reviewer's open Stage-6 ask). Existing Phase 4e (multi-effect handles) renumbered to **Phase 4f**. Existing Phase 4f (return arms) renumbered to **Phase 4g**. Renumber reflects priority: Phase 4e closes a correctness gap, Phases 4f/4g are feature breadth.
  - **Phase 4f PR #28** (`plan-b-task-55-phase-4f` branch against `main` post-PR-#27-merge; HEAD = closeout commit; CI green; awaiting squash-merge). Eight commits shipped per `[DEVIATION Task 55] Phase 4f` in `PLAN_B_DEVIATIONS.md` — Option A (push N frames at handle entry) over Option B (extend `HandlerFrame`); reversibility-led architecture (Phase 4f-2 escape valve documented); frame_1 pointer snapshot serves both the discipline check (concern #1) and Phase 4g's return-arm contract (concern #2); five pre-registered concerns + four hard conditions all closed:
    - **Foundation** (`8605d9d`) — deviation entry + PROGRESS update. No source code changes. Architecture rationale leads with reversibility (B can be layered on top with a clean ABI version bump if A turns out wrong).
    - **Foundation review fixups** (`b2afc65`) — addresses 6 substantive items + 2 polish-round forward concerns from the foundation mid-flight reviews. Concern #1 debug-assert FFI mechanism pinned (no new FFI required); concern #2 frame_1 pointer snapshot at push time pinned; arm_count semantic clarification (per-frame vs per-handle); IR-equivalence claim downgraded; stale-ref sweep pre-enumerated; line-number rot fixed.
    - **Foundation re-review fixup** (`4e74683`) — replaces broken `.prev`-comparison concern #1 mechanism (would have misfired 100% of the time on the happy path because `sigil_handle_pop` zeros `prev` at `runtime/src/handlers.rs:451`) with the **frame_1 pointer comparison** mechanism. Same snapshot, two purposes (concern #1 + concern #2). Renumber-victim at `codegen.rs:712-714` flagged.
    - **Codegen lift** (`65727c2`) — drops the `op_arms[0].effect` consistency check; rewrites `Expr::Handle` lowering: BTreeMap-grouped per-effect alloc-set-push, `frame_1_ptr_snapshot: Option<Value>` captured on first frame, reverse-pop at handle exit, `cfg!(debug_assertions)`-gated `TRAP_HANDLE_DISCIPLINE_VIOLATION = 0x42` icmp+brif at handle exit. New trap code constant. The same-effect rejection block removal incidentally closes hard condition #4 (codegen.rs stale-`Phase 4e` refs at `:715-716`, `:746`, `:755` all eliminated). Renumber-victim at `:712-714` fixed in passing. **Hard condition #1 closed:** rejection test inverted to `handle_with_mixed_effect_arms_dispatches_correct_arm_per_effect` exercising `Foo` + `Bar` with sentinel-99 misdispatch detection (asserts `stdout="18\n"`).
    - **Codegen-lift CI fix** (`76e89d8`) — `nested_handle_in_outer_body_propagates_inner_unsupported_diagnostic` was using inner-handle-multi-effect as its walker-recursion regression sentinel; Phase 4f lifted that sentinel. Swapped to inner-handle-with-`return`-arm (still Phase 4g pending); walker-recursion coverage intent preserved.
    - **Codegen-lift review fixup** (`38c9429`) — addresses both mid-flight reviews of the codegen-lift commit. Misleading "today's callers satisfy" comment on op_id/arm_count rewritten with the accurate constraint description. **Latent op_id/arm_count constraint** documented as a sub-section in the deviation entry: handlers must currently cover op_ids `[0, k)` contiguously for the runtime bounds check to pass; partial-handler-of-multi-op-effects programs runtime-abort. Pre-existing since Phase 4a; Phase 4f doesn't introduce or fix it but expands the user-reachable surface. Both resolution options (option 1: convention fix sizing arm_count to effect's declared op count; option 2: typecheck E0142 exhaustiveness) flagged for a separate post-Phase-4f task. `#[ignore]`'d pinning test `partial_handler_of_multi_op_effect_aborts_at_runtime_pending_resolution` mirrors the Phase 4d MVP `discard_k_handler_does_not_abort_helper_phase_4e_pending` precedent.
    - **Polish round** (`c11f45f`) — `MAX_HANDLER_ARMS = 14` promoted from `sigil_runtime::handlers` to `sigil_abi::effect` (mirrors Phase 4b's `MAX_INLINE_ARGS` precedent); runtime re-exports via `pub use` for backwards-compat. Codegen-walker per-effect arm-count cap check (`BTreeMap<&str, u32>` counter) rejects `MAX_HANDLER_ARMS+1` arms-per-effect at compile time with a clean diagnostic instead of letting `sigil_handler_frame_new` runtime-abort. Four polish-round e2e tests: (a) 2 effects × 2 arms each (`handle_with_two_effects_two_arms_each_dispatches_per_op` — closes the per-effect-multiple-arms gap left by the inversion test); (b) source-order independence (`handle_with_arms_in_reverse_source_order_produces_same_output` — pins frame-push order to effect-id-lex-order); (c) MAX_HANDLER_ARMS positive (`handle_with_one_effect_at_max_handler_arms_compiles_and_dispatches` — multi-effect handle with 14 Wide-effect arms + 1 Other-effect arm = 15 collective, per-frame cap = 14 accepts; demonstrates the cap is per-frame); (d) MAX_HANDLER_ARMS+1 negative (`handle_with_one_effect_exceeding_max_handler_arms_is_rejected_at_codegen` — 15 TooWide-effect arms compile-rejected with diagnostic naming `MAX_HANDLER_ARMS` and the offending effect).
    - **Closeout** — **Hard condition #2 closed:** README "Verification limits" multi-effect row flipped to "Closed at PR #28" with prose pointing at the deviation entry's architectural rationale; Phase 4g (return arms) becomes the only remaining feature-breadth gap in the table. **Hard condition #3 closed:** this PROGRESS entry flipped from `in-progress` to `done-pending-ci` with the eight-commit list above. **Squash-merged at `08d002a` on 2026-04-28** as `[Task 55] Phase 4f: multi-effect handlers via push-N-frames (#28)`. Branch `plan-b-task-55-phase-4f` deleted on origin (auto-cleanup post-merge); local branch deleted.
    
    Hard conditions all closed: (1) test inversion ✅ at codegen lift; (2) README "Verification limits" ✅ at closeout; (3) PROGRESS Phase 4f entry filled with implementing-commit list ✅ at closeout (squash-hash adds post-merge); (4) walker / codegen stale-`Phase 4e` reference sweep ✅ implicitly via codegen-lift rejection-block removal + renumber-victim fix-in-passing.
    
    **Phase 4f-cleanup deferred items (separate post-Phase-4f tasks):** (1) `scope_id` per-frame field for diagnostic precision when an unhandled op walks past N frames of the same handle (concern #3 + #5; ABI-adjacent; deferred until concrete motivation surfaces); (2) latent op_id/arm_count constraint resolution (option 1 convention fix or option 2 typecheck E0142; `#[ignore]`'d pinning test `partial_handler_of_multi_op_effect_aborts_at_runtime_pending_resolution` un-ignores at the fix PR).
  - **Phase 4g PR #29** (`plan-b-task-55-phase-4g` branch against `main` post-PR-#28-merge; **squash-merged at `a777748` on 2026-04-28**). Six commits squash-merged per `[DEVIATION Task 55] Phase 4g` in `PLAN_B_DEVIATIONS.md` — Option A (codegen-driven dispatch, no new FFI) over Option B (runtime-driven `sigil_handle_pop_with_return`); concern #2 of the Phase 4f deviation entry pre-pinned this contract — the first-pushed-frame is the durable anchor for return-on-body-Done semantics. Synth return fn uses uniform CPS calling convention; `args_ptr=[v_widened, post_handle_k_closure, post_handle_k_fn]` (Phase 4e Slice A's trailing-pair convention applied uniformly); surrounding-fn dispatch at handle exit always passes `(null, &sigil_continuation_identity)` as the trailing pair, so the trampoline collapses to `Done(widened)`. Five pre-registered concerns + four hard conditions all closed.
    - **Foundation** (`5b30601`) — deviation entry + PROGRESS update + Phase 4f post-merge hash flip (`done-pending-ci` → squash-merged at `08d002a`). No source code changes. Architecture rationale leads with the no-new-FFI choice and the first-pushed-frame contract inheritance.
    - **Codegen lift** (`eabef59`) — drops the `return_arm.is_some()` rejection at `compiler/src/codegen.rs:733-740` (block deleted; surrounding comment updated to past tense). Adds `HANDLER_FRAME_RETURN_FN_OFF = 8` / `HANDLER_FRAME_RETURN_CLOSURE_OFF = 16` constants in `sigil_abi::effect` (compile-asserts pinning offsets to `offset_of!(HandlerFrame, ...)`). New `CheckedProgram::handle_return_arm_captures: BTreeMap<Span, Vec<(String, Ty)>>` typecheck side-table populated during `check_handle`'s return arm walk against the saved env. New `HandlerReturnArmSynth` struct + parallel `handler_return_arm_synth: Vec<HandlerReturnArmSynth>` and `handler_return_arm_indices: BTreeMap<Span, usize>` codegen pre-pass tables. Pre-pass allocates one return-arm `FuncId` (mangled `sigil_handler_return_arm_<global_idx>`) when `return_arm.is_some()`, rewrites the body via `rewrite_arm_body_with_captures` (binding name as single arg, empty `k_name`). Walker `arm_body_walk` reused for return-arm body validation with `k_name = ""` (return arms have no continuation binding so the k-related branches are inert) and a single scope frame containing the `v` binding name; rejects nested `Lambda` / `ClosureRecord` (mirrors op-arm restriction). Nested `Expr::Handle` allowed as a freebie — Phase 4f's machinery extends transparently via `Lowerer::lower_expr`'s recursive arm; pre-pass already recurses into return arm bodies. New synth fn definition pass mirrors the arm-fn body emit (single user arg `v` instead of N op-args; no `k`-tail branching). `Lowerer::lower_expr`'s `Expr::Handle` arm extended: after the Phase 4f reverse-pop loop, when `return_arm.is_some()`, build `NextStep::Call(return_closure, return_fn, 3)` with trailing-pair payload `[body_val_widened, null, identity_fn_addr]`, drive `sigil_run_loop`, narrow result via `type_of_expr(&ra.body, &preview)` (preview binds `v` to body's actual Cranelift type). `set_return` registered on the **first-pushed frame** of the handle's frame group (per concern #2's contract); return-arm closure record alloc'd via Phase 4d's `alloc_arm_closure_record` when captures present. New `Lowerer` fields: `next_step_call_ref`, `next_step_args_ptr_ref`, `handler_frame_set_return_ref`, `handler_return_arm_refs_per_handle`, `handler_return_arm_synth`, `handler_return_arm_indices`. 8 new e2e tests: (a) `nested_handle_in_outer_body_propagates_inner_unsupported_diagnostic` INVERTED to positive (asserts `1\n` from `handle (handle 0 with { return(v) => v + 1, ... }) with { ... }`); (b) `handle_with_return_arm_transforms_body_value_no_op_arms_fired` — happy-path (asserts `11\n` from `5*2+1`); (c) `handle_with_return_arm_op_arm_fires_return_arm_skipped` — pins op-arm-fires-skips-return semantics (asserts `99\n`, not `9900\n`); (d) `handle_with_return_arm_captures_outer_fn_local` — captures outer fn's `scale` (asserts `28\n` from `4*7`); (e) `handle_with_return_arm_in_multi_effect_handle_first_frame_contract` — pins first-pushed-frame contract (asserts `30\n` from `3*10`); (f) `handle_with_return_arm_body_performs_io` — return arm body performs IO (asserts `done\n42\n`); (g) `handle_with_return_arm_body_type_differs_from_body_type` — body Int, return arm body Bool ⇒ handler-overall Bool (asserts `big\n`, exercises I64→I8 narrow-back); (h) `handle_with_return_arm_inside_op_arm_chain_runs` — both op + return arms declared, op arm fires (asserts `7\n`); (i) `nested_handle_with_inner_lambda_in_arm_body_is_rejected_at_codegen` — new walker-recursion regression sentinel (replaces the inverted nested-handle-return-arm sentinel); inner handle with Lambda in arm body still rejected. Pod-verify clean throughout; runtime test count 64 → 68 (+1: `handler_frame_return_offsets_match_abi_constants` pinning ABI offsets to `offset_of!`).
    - **Closeout (this commit)** — README "Verification limits" return arms row flipped to "Closed at PR #29" with prose pointing at the deviation entry's architectural rationale; PROGRESS Phase 4g entry filled with implementing-commit list ✅ at this commit (squash-hash adds post-merge); deviation entry's concern #5 reframed (nested handle in return arm body supported as a freebie, not deferred). Tasks 57–61 + Stage 6 review checkpoint are now the only remaining Plan B work — all Phase 4 sub-work for Task 55 is complete.

    Hard conditions all closed: (1) walker rejection lifted ✅ at codegen lift; (2) README "Verification limits" ✅ at closeout; (3) PROGRESS Phase 4g entry filled with implementing-commit list ✅ at closeout (squash-hash adds post-merge); (4) bisecting-hint pattern in deviation entry naming three Phase 4g failure modes a future bisecting agent should attribute to this PR vs Phase 4e/4f.
  - **Phase 4e is a Stage 9 prerequisite (HARD GATE) — closed at `3affced` on 2026-04-28.** Split across PR #26 (closed at `2a3cb25`) + the captures+ follow-up PR per the cadence pivot. PR #26 closed the discard-`k` correctness gap (the load-bearing semantic gap). The captures+ PR closes multi-shot `k` (P20 `Choose`) and the residual non-tail-`k` / surrounding-lambda gates. Stage 9 unblocks per the captures+ squash-merge. The validation prompt bank's algebraic-effects entries (P6 `Raise[String]`-based safe parser, future P19 `State[Int]` counter, future P20 multi-shot `Choose`) all become measurable after the captures+ squash-merge. **Note:** P19 and P20 prompt entries do not yet exist in `spec/validation-prompts.md` (only P01–P17); they're forward-looking framings against future prompt-bank additions, not current ones. Concrete P20 verification against Slice C v1's 2-let surface deferred to when the prompt lands. `scripts/validate-spec.sh` (Stage 9 of `2026-04-21-sigil-finish.md`, plan items 85–87) cannot run until Phase 4e ships. The validation prompt bank's algebraic-effects entries (Plan A/B-bank P6 `Raise[String]`-based safe parser, P19 `State[Int]` counter threaded through a list walk, P20 multi-shot `Choose` for all-pairs-summing-to-N) all exercise patterns the Phase 4d MVP synchronous shape silently miscompiles when the perform reaches the arm via a function-call boundary. Specifically: a discard-`k` arm (the canonical Raise pattern) does not abort the helper fn's post-perform code under the MVP; it only happens to produce the right answer when the perform is in tail position of the handle body (every existing Phase 4c e2e test). Cross-function-call discard-`k` correctness needs the colorer's handler-discharge refinement so native-color helper fns whose performs reach a discharging handler reclassify as CPS-color and return `NextStep::Call` to the enclosing trampoline rather than synchronously blocking on `sigil_run_loop`. Stage 9's pass-rate thresholds (70% first-compile / 90% after-one-edit on Opus AND Sonnet) cannot be met if `Raise`-style handlers silently produce wrong results — fresh-Claude-session validation will write programs that assume standard algebraic semantics. The README's "Verification limits" section (landed in the same PR as the Phase 4d MVP) documents the gap user-facing; the `#[ignore]`'d e2e test `discard_k_handler_does_not_abort_helper_phase_4e_pending` pins the broken behavior in CI and inverts to a normal test when Phase 4e lands.
  - commits: [b3af204, 2d69b52, ef4be8d, d0aa4c4, 2e7c0de, adcb897, 2114235, e3ed53a, HEAD]
  - Phase 4d MVP (`HEAD` on `plan-b-task-55-phase-4d`): closure-capture from surrounding-fn locals + tail-position `k(arg)` via `sigil_continuation_identity` runtime intrinsic. **Typecheck:** new `CheckedProgram::handle_arm_captures: BTreeMap<Span, Vec<Vec<(String, Ty)>>>` side-table, populated in `check_handle` against the saved env (the surrounding fn's lexical scope at the handle expression, before arm-param/k bindings install). **Codegen pre-pass:** `HandlerArmSynth` extended with `captures: Vec<ArmCapture>` and `k_name: String`; new `rewrite_arm_body_with_captures` rewrites every captured-name `Expr::Ident` (and any closure_convert-rewritten `Expr::ClosureEnvLoad` with the same name) into an arm-local-indexed `Expr::ClosureEnvLoad`; new `arm_body_tail_is_k_call(body, k_name)` detects the tail-`k(arg)` shape recursing through `Expr::Block` tails. **`Expr::Handle` lowering:** when an arm has captures, codegen calls new `alloc_arm_closure_record(captures)` (mirrors `lower_closure_record` — TAG_CLOSURE header + bitmap from `EnvSlotKind::is_pointer()`, code_ptr slot null, env slots widened per kind, env values sourced from `Lowerer.env`); the resulting closure_ptr replaces Phase 4c's null_ptr in `sigil_handler_frame_set_arm`. `lower_perform_non_io_to_value` replaces null_k_fn with `func_addr(continuation_identity_ref)` (k_closure stays null since identity is closure-less). **Synth-pass:** arm fns now also load `(k_closure, k_fn)` from `args_ptr[N], args_ptr[N+1]` (N = `arg_names.len()`), matching the Task 56 ABI; body lowering routes via `arm_body_tail_is_k_call`: tail-k path lowers pre-tail stmts via new `lower_arm_body_pre_tail_k_stmts`, lowers the arg in non-tail position, widens to I64, builds `NextStep::Call(k_closure_loaded, k_fn_loaded, /*arg_count=*/1)` via `sigil_next_step_call`, writes widened arg via `sigil_next_step_args_ptr`, returns the NextStep ptr; non-tail path keeps Phase 4c flow (lower_expr → widen → sigil_next_step_done → return). **Walker (`arm_body_phase_4c_violations`) restructure:** new `tail: bool` parameter threads through; `Expr::Ident` capture rejection lifted (surrounding-fn locals now allowed); `Expr::ClosureEnvLoad` rejection preserved with re-aimed Phase 4e diagnostic (handle-inside-lambda surrounding-fn-capture access requires a closure-convert side-table extension); k-name rejection split into tail-OK + non-tail-rejected; nested `Lambda` / `ClosureRecord` rejection preserved. **Runtime:** new `sigil_continuation_identity(closure_ptr, args_ptr, args_len) -> *mut NextStep` returns `sigil_next_step_done(*args_ptr)` — codegen emits its address as k_fn at every non-IO perform site so a tail-k arm fn's `sigil_next_step_call` dispatches into the identity continuation, producing terminal `Done(arg)`. **Pipeline plumbing:** `closure_convert::slot_kind_for_ty` made `pub(crate)` for codegen reuse; `color.rs` test fixture + `Tc::fresh_tc` helper extended with the new field. **Tests:** 2 rejection-tests converted to positive (`arm_uses_k_in_tail_position_returns_continuation_value` printing `99`, `arm_captures_outer_scope_returns_value` printing `42`); 1 rejection-test re-aimed at Phase 4e (`arm_inside_lambda_captures_outer_via_closure_env_load_is_rejected_at_codegen_phase_4e_pending`); 1 new rejection (`arm_uses_k_in_non_tail_position_is_rejected_pointing_at_phase_4e`); 1 `#[ignore]`'d Phase 4e pinning test (`discard_k_handler_does_not_abort_helper_phase_4e_pending`) — pins the discard-k-cross-call-boundary semantic gap; inverts to a positive test (asserting stdout `42`) when Phase 4e ships the colorer's handler-discharge refinement. Pod-verify clean. See `[DEVIATION Task 55] Phase 4d MVP scope` in `PLAN_B_DEVIATIONS.md` for the architectural choices (MVP-vs-full split rationale; `sigil_continuation_identity` design notes; closure-record alloc site; bisecting-hint pattern); the README "Verification limits (in-flight)" section documents the user-facing semantic gaps deferred to Phase 4e.
  - Phase 4c (`e3ed53a`): richer arm bodies via Lowerer; arm-side arg unpacking; perform-side return-type narrowing. `HandlerArmSynth` extended with `arg_names: Vec<String>`, `arg_types: Vec<Type>`, `body_ty: Type` (resolved from each arm's matching `EffectOp` declaration via `cranelift_ty_for_type_expr`). The pre-pass `collect_handle_arms_in_*` family threads a new `ArmSynthCtx` (carries `cps_arm_sig`, `op_ids`, `effects`, `pointer_ty`) so per-arm metadata is populated alongside `FuncId` allocation. The synthetic-fn definition pass at the bottom of `emit_object` is rewritten: each arm fn now declares the same per-fn FFI refs / lit_gvs / user_fn_refs / handler_arm_refs as a regular user fn, loads each op-arg from `args_ptr` at fn entry (`load.i64` then `ireduce` to declared type for narrower Bool/Byte/Unit/Char), binds them in a fresh `Lowerer` env, lowers the arm body via `lower_expr`, widens the body value to I64 (matching `sigil_next_step_done`'s I64 signature), and wraps via `sigil_next_step_done(value)` + return. **`lower_perform_non_io_to_value` now narrows** the I64 returned by `sigil_run_loop` back to the op's declared return type (looked up via `effects[effect].ops[op].return_type`), with an `assert!` fallthrough for unexpected types — mirrors the perform-side widening discipline. The `unsupported_handle_construct` walker drops the IntLit-only restriction and replaces it with three new gates implemented in `arm_body_phase_4c_violations` (scope-tracking walk over the arm body, with op-args at the bottom of the scope stack and let / pattern / nested-handle bindings pushed/popped as scopes open/close): (1) k-name references (rejected with a Phase-4d-pointing message), (2) outer-scope captures (rejected with a Phase-4d/onward-pointing message), (3) nested `Lambda` / `ClosureRecord` (same closure point as captures). The walker now collects globals (top-level fn names + ctor names + `int_to_string` builtin) once at entry and threads them through. Seven new e2e tests, four of which close the **Phase 4b deviation entry's pre-registered acceptance precondition**: `arm_reads_int_arg_returns_it`, `arm_reads_bool_arg_branches_on_it`, `arm_reads_string_arg_prints_via_io_println`, `arm_reads_multi_args_in_declared_order`. Three bonus tests cover richer shapes the Lowerer now supports: `arm_body_does_arithmetic_on_op_args` (multi-arg arithmetic in arm body), `arm_uses_k_is_rejected_at_codegen` (k-usage gate), `arm_captures_outer_scope_is_rejected_at_codegen` (capture gate). Pod-verify clean. See `[DEVIATION Task 55] Phase 4c — richer arm bodies via Lowerer; arm-side arg unpacking; perform-side return-type narrowing` for the architectural choices (shared Lowerer vs dedicated CPS-aware lowerer; widen/narrow mirror discipline; scope-tracking walker).
  - Phase 4b (`2114235`): args-buffer packing on perform side. `MAX_INLINE_ARGS` (32) moved from `sigil_runtime::handlers` to `sigil_abi::effect` so codegen and runtime read from one source; the runtime now `pub use`-re-exports it for backwards-compat with existing callers. `lower_perform_non_io_to_value` now lowers each user arg, widens narrower Cranelift types to `u64` (`uextend` for I8 Bool/Byte/Unit + I32 Char; pointer-typed args store directly because pointer_ty == I64 on supported targets), stores them into a stack-allocated `[u64; N]` slot at offsets 0, 8, 16, ..., and passes the slot's `stack_addr` + `args_len` to `sigil_perform` (which copies them into the dispatched `NextStep::Call`'s args slots before invoking the arm fn). A defensive compile-time `debug_assert!` mirrors the runtime's `args_len + 2 > MAX_INLINE_ARGS` overflow check. The `unsupported_handle_construct` walker dropped both the `arm.params.is_empty()` gate and the `body_contains_non_io_perform_with_args` gate; the now-dead `body_contains_non_io_perform_filtered` / `block_contains_non_io_perform_filtered` helpers were removed. Three new e2e tests: `handle_with_int_arg_op_packs_args_buffer` (single Int arg), `handle_with_three_int_args_packs_buffer` (multi-arg packing), `handle_with_mixed_type_args_widens_correctly` (widening: Int + Bool + String). Args-content correctness is a pre-registered Phase 4c acceptance precondition (4 named tests) — closed at HEAD. See `[DEVIATION Task 55] Phase 4b — args-buffer packing on perform side; stack-slot allocation; arm side still IntLit-only`.
  - Phase 4a (`adcb897`): multi-arm single-effect handlers. Codegen-entry guard relaxed to allow `op_arms.len() > 1` when every arm targets the same effect; mixed-effect handlers and duplicate `(effect, op)` arm pairs continue to be rejected (the latter is a typecheck E0140 already; codegen guards defensively). New e2e tests: `handle_with_two_arms_dispatches_correct_arm_by_op_id` pins per-op dispatch; `handle_with_mixed_effect_arms_is_rejected_at_codegen` pins the Phase 4e blocker. Phase 3b's per-arm CPS fn synthesis already iterates arms — the relax was ~50 LOC of guard changes, no new codegen machinery.
  - Phase 3b fixup (`2e7c0de`): the initial Phase 3b commit (`d0aa4c4`) extracted the perform's value via a direct `(*next_step).value` read assuming Done; CI revealed the path actually returns `NextStep::Call` (sigil_perform builds the Call to the arm + packed `(args, k)` and returns it without invoking). Fixup added the `sigil_run_loop` FFI declaration and routed perform's return through it; run_loop dispatches the Call (and any further calls the arm returns) until terminal `Done(value)` and returns the value as u64. Codegen no longer reads the `NextStep` layout directly. The same dispatcher will work in Phase 4d when arms chain into further calls via `k`.
  - Phase 3b (`d0aa4c4`): real handler dispatch end-to-end. Per-handler-arm CPS fn synthesis (`HandlerArmSynth` struct + `collect_handle_arms_in_block`/`_in_expr` pre-pass + dedicated definition pass at the bottom of `emit_object`). 3 new FFI declarations: `sigil_handler_frame_set_arm`, `sigil_perform`, `sigil_next_step_done`. `Expr::Handle` codegen now sets each arm's fn pointer via `func_addr` + `sigil_handler_frame_set_arm` between `frame_new` and `push`. `lower_perform` extended with `lower_perform_non_io_to_value`: emits `sigil_perform(effect_id, op_id, args_ptr=null, args_len=0, k_closure=null, k_fn=null)`, then hands the returned NextStep off to `sigil_run_loop` (added in the Phase 3b fixup commit `2e7c0de`) to drive the trampoline to a terminal `Done(value)`. `Expr::Perform` arm in `lower_expr` dispatches: IO → existing path returning Unit (i8 0), non-IO → CPS path returning the value. `type_of_expr` for `Expr::Perform` looks up the op's declared return type from `effects` map for non-IO; defaults to I8 for IO. New e2e test `handle_with_non_io_perform_runs_arm_and_returns_value` (the load-bearing milestone — first program where a real handler arm is dispatched at runtime; prints `42`). The Phase 2 `handle_with_no_perform_in_body_compiles_and_runs` continues to pass. The `handle_with_non_io_perform_in_body_is_rejected_at_codegen` test from Phase 2 was replaced by `handle_with_non_intlit_arm_body_is_rejected_at_codegen` pinning the new Phase 3b restriction (arm body must be `Expr::IntLit`; `k`-using or richer arms still rejected with a Phase-4-pointing message). Pod-verify clean.
  - Phase 3a (`ef4be8d`): wires the runtime handler-frame ABI from Task 56 around every `handle` body. New FFI declarations for `sigil_handler_frame_new(effect_id, arm_count) -> *mut HandlerFrame`, `sigil_handle_push(frame)`, `sigil_handle_pop() -> *mut HandlerFrame`. `Expr::Handle` codegen now allocates a frame, pushes it, evaluates the body inline, then pops the frame. Arm fn pointers are not yet set (zero-init from `sigil_handler_frame_new` leaves them null), which is safe because the codegen-entry guard `unsupported_handle_construct` rejects programs whose body would actually `perform` the handled effect — the runtime never reads an arm slot for these handles. Phase 3a tightens the guard to also reject multi-arm handles, return arms, and (defensively) zero-arm handles, so the codegen path only takes single-arm/single-effect/no-return handles. The Phase 2 e2e test `handle_with_no_perform_in_body_compiles_and_runs` continues to pass (now exercising the frame ABI for the first time end-to-end). `Lowerer` gained `handler_frame_new_ref`/`handle_push_ref`/`handle_pop_ref` FuncRef fields plus `effect_ids: &BTreeMap<String, u32>` for effect-id lookup. Phase 3a has a tiny runtime regression — the no-perform handle now allocates + pushes + pops a frame on every invocation (previously a no-op pass-through) — acceptable as the cost of exercising the FFI plumbing on the path Phase 3b builds on. Pod-verify clean.
  - Phase 2 minimum (`2d69b52`): both staged gates lifted; effect/op IDs assigned at typecheck; `Expr::Handle` initial codegen as body-pass-through when the body has no non-IO `perform`; codegen-entry guard `unsupported_handle_construct` rejects programs whose body would actually perform a non-IO effect with a clear in-progress message. `type_of_expr` for `Expr::Handle` forwards to body's type. New e2e tests pin both paths: `handle_with_no_perform_in_body_compiles_and_runs` (handle expression compiles + runs end-to-end for the first time, prints `42`), `handle_with_non_io_perform_in_body_is_rejected_at_codegen` (clean codegen-time rejection + error references Task 55 / CPS). `effect_ids: BTreeMap<String, u32>` and `op_ids: BTreeMap<(String, String), u32>` added to `CheckedProgram` with deterministic alphabetical assignment; 3 new typecheck tests pin the assignment scheme. E0134 emission removed from `check_handle`; catalog entry removed; the four asymmetric-state tests from `b3af204` rewritten as positive `well_formed_handle_expr_typechecks_cleanly` + co-firing-diagnostic tests. Discipline sweep `no_user_facing_error_uses_e0001` updated. Pod-verify clean.
  - notes: Task 55 is the largest remaining chunk in Plan B and will land across multiple commits on `plan-b-task-55`. **Foundation phase (this commit)** lifts the E0133 staged-feature gate and wires effect declarations through the codegen entry walker as a no-op item kind (they only populate the typecheck-time effect registry consulted by `perform` dispatch + the runtime handler-stack ABI from Task 56; downstream passes — monomorphize, color, closure_convert — already pass `Item::Effect` through unchanged from Task 54). **Asymmetric gate state** kept intentionally: E0133 lifted, E0134 still firing. Well-formed effect-only programs (no `handle`, no non-IO `perform`) compile end-to-end through codegen for the first time. Programs that use `handle` still surface E0134 with an updated message pointing at this in-progress task — they will reach the still-`unreachable!` codegen arms in `lower_expr`/`type_of_expr` once E0134 lifts, so the gate stays live until the CPS calling convention + runtime FFI wiring lands. New e2e test `effect_decl_with_no_handler_use_compiles_and_runs` pins the new path. `compiler/src/codegen.rs::contains_apply_or_generic_ref` `Item::Effect → return true` short-circuit removed (line 200); replaced by `Item::Effect(_) => {}` skip with explanatory comment. `compiler/src/typecheck.rs` E0133 emission removed (line 612 area); E0134 emission retained at the bottom of `check_handle` (line 3040 area) with updated message and rationale. Catalog entry for E0133 removed (`compiler/src/errors/catalog.rs`); E0134 entry kept with rewritten long-form text noting the in-progress status. Test cleanup via `perl -i -ne 'print unless /assert!\(has_code\(&errs, "E013[34]"\), .../...'` stripped 21 co-firing `assert!` lines (19 E0134 + 2 E0133); the four pure-emission tests at lines 6885 / 6899 / 6910 / 6944 rewritten to test the new asymmetric state — `well_formed_effect_decl_typechecks_cleanly` and `effect_decl_with_invalid_op_type_emits_e0112` for the lifted half, `handle_expr_still_emits_e0134_until_codegen_lands` and the two co-firing variants for the still-gated half. New positive walker test `walker_accepts_program_with_effect_decl` verifies the codegen entry-walker case directly. Discipline-test sweep `no_user_facing_error_uses_e0001` programs unchanged; comment updated to describe the asymmetric state. **Pod-verify clean** (cargo check workspace, fmt, clippy on both crates, runtime lib tests). Compiler lib-test count delta: net 0 (4 pure-emission tests rewritten in place + 1 new walker test = +1 from baseline). **Remaining Task 55 work** (deferred to follow-up commits on this same branch): full CPS calling convention for CPS-color fns (`extern "C" fn(closure_ptr, args_ptr, args_len) -> *mut NextStep`), CPS transform of fn bodies in `cps.rs` (currently a 21-line pass-through stub), per-handler-arm closure synthesis, handler-frame allocation and push/pop in `Expr::Handle` codegen, `sigil_perform` call emission for non-IO `Expr::Perform`, native↔CPS interop wrappers, trampoline integration for CPS-color `main`, `CpsCallCount` / `NativeCallCount` counter wiring, and the E0134 gate lift. PR opens once the full path actually compiles + has at least one e2e test exercising a real handler.
- Task 56 — Runtime: `HandlerFrame`, arena, `sigil_perform`, `run_loop`, counters
  - status: done-pending-ci
  - commits: [9c6213e]
  - notes: Lands ahead of Task 55 — see [DEVIATION Task 56] "Task 56 lands before Task 55" in PLAN_B_DEVIATIONS.md. Implements the runtime side of the algebraic-effects subsystem in isolation (Task 55's codegen wires through to the FFI surface in a follow-up PR). New `sigil_abi::effect` module pins the `NEXT_STEP_TAG_DONE` / `NEXT_STEP_TAG_CALL` discriminants as the single cross-boundary `NextStep` value the compiler reads directly; the runtime owns the rest of the `NextStep` and `HandlerFrame` byte layouts behind FFI helpers. New `runtime/src/handlers.rs` module: `NextStep` and `HandlerFrame` `#[repr(C)]` structs (variable-length `arms` array via raw-pointer accessors), thread-local handler-stack head Cell, `sigil_handler_frame_new` / `_set_arm` / `_set_return` allocation+population helpers (frame is `sigil_alloc`-Boehm-allocated under TAG_CLOSURE reuse with a precise GC pointer bitmap covering `return_closure`, `prev`, and per-arm `closure_ptr` slots; `MAX_HANDLER_ARMS = 14` set by the 32-bit bitmap and forward-compat with v1 effect arities), `sigil_handle_push` / `sigil_handle_pop` stack management, `sigil_perform(effect_id, op_id, args_ptr, args_len, k_closure_ptr, k_fn_ptr) -> *NextStep` linear walk dispatching to the topmost matching frame's arm with `(args, k)` packed, `sigil_next_step_done(value)` and `sigil_next_step_call(closure, fn, arg_count)` arena-allocators returning `*mut NextStep` pointers, `sigil_next_step_args_ptr` accessor, and `sigil_run_loop(initial)` dispatcher that resets the arena per iteration after copying dispatch info into stack locals (the non-reallocating arena invariant guarantees pointers stay valid across in-iteration allocations). `runtime/src/arena.rs` replaces the abort stub with a thread-local `Vec<u8>` bump allocator (`INITIAL_CAPACITY = 64 KiB`, 8-byte aligned, non-reallocating set_len pattern, abort on overflow per the documented capacity discipline) plus `sigil_arena_promote(src, header, payload_bytes)` for Task 55's escape sites. **Counter wiring this task:** `ArenaAllocCount` / `ArenaAllocBytes` (per `sigil_arena_alloc`), `ArenaEscapeCount` (per `sigil_arena_promote`), `HandlerWalkCount` / `HandlerWalkDepthSum` (per `sigil_perform` — depth includes the matching frame), `TrampolineDispatchCount` (per `sigil_run_loop` iteration). `CpsCallCount` / `NativeCallCount` left untouched here — they live at codegen entry and ship with Task 55. **Three deviation entries** in PLAN_B_DEVIATIONS.md: (1) order swap (Task 56 before Task 55) with the rationale matrix; (2) uniform CPS calling convention `extern "C" fn(closure_ptr, args_ptr: *const u64, args_len: u32) -> *mut NextStep` chosen over typed direct calls so the trampoline's dynamic dispatch is portable across both hosts without per-arity dispatch / hand-rolled assembly / per-fn thunk indirection; (3) HandlerFrame TAG_CLOSURE reuse with the precise bitmap rationale and the `MAX_HANDLER_ARMS = 14` cap. **34 new runtime unit tests** (5 arena + 18 handlers + 11 layout/counter assertions) covering: arena alloc round-trip and 8-byte alignment, reset-clears-len-preserves-capacity, write-observability through returned pointer, the load-bearing non-reallocating-pointer-stability invariant (512-iteration stress), promote-copies-bytes (with TAG_INT64 stand-in header under the gc_test_lock), counter delta minimum-bounds (counter races inflate, never deflate), `NextStep::Done` and `NextStep::Call` round-trips with packed args, `sigil_run_loop` dispatch on terminal Done / single Call+Done / chained 2-step Call+Call+Done with explicit dispatch-count assertions, HandlerFrame allocation with zero arms / max arms / arm-slot zero-init, `set_arm` / `set_return` writes at the documented offsets, push/pop round-trip with `prev` chain validation, `sigil_perform` direct dispatch and walk-past-unrelated-outer-frame with depth-counter assertions, and full args-layout assertion (`(user_args..., k_closure, k_fn)`). Pod-verify clean: cargo check workspace, fmt, clippy on both crates, runtime lib test suite (54 -> 58 passing tests, 0 failed, 0 ignored), `scripts/check-no-interior-pointers.sh` passing (`SAFETY: not an interior pointer` annotations on every arena/handler pointer-arithmetic site).
- Task 57 — Refactor IO shortcut; `Raise[ArithError]` replaces `sigil_panic_arith_error`
  - status: done (squash-merged at `7870eea` on 2026-04-28 as PR #30)
  - commits: [d2828fa, 8311723, b98c08a, 29b2b5e, 2e12eaa, 28721af, c1916b4, 3ea96cc, squash → 7870eea]
  - notes: Two-slice refactor on PR #30 (`plan-b-task-57` branch against `main`). **7 commits squash-merged**:
    - **Foundation** (`d2828fa`) — 5 deviation entries (builtin-effect injection vs. full stdlib loading; ArithError op return type `Int` v1 over `Never`; top-level handler installation in main shim; `BinOp::Div`/`Mod` introduce ArithError into rows; single-PR with two commit slices) + Phase 4g squash-hash flip (`done-pending-ci` → squash-merged at `a777748` on 2026-04-28). No source code changes.
    - **Foundation review fixups** (`8311723`) — addresses 7 items from the foundation mid-flight reviews: ArithError gains `mod_by_zero` op (review-2 issue #1; preserves Plan A2's distinct `%`-vs-`/` stderr message); reserved-low-id convention pinned (review-2 issue #2; `ArithError = 0`, `IO = 1` regardless of user-effect count); elaborate-time synthesis adopted over codegen-time (review-1 forward concern #1; colorer sees the perform structurally); e2e row-update count quantified (review-2 issue #3; 3 source files); shim discipline check parity adopted (review-1 forward concern #3); inbound-vs-outbound `args_ptr` references renamed (review-2 issue #5); inherited `MAX_HANDLER_ARMS = 13` → 14 drift fixed in PROGRESS + DEVIATIONS (review-2 issue #6).
    - **Slice 1: IO refactor** (`b98c08a`) — synthetic builtin `effect IO { println }` + `effect ArithError { div_by_zero, mod_by_zero }` injected at typecheck pre-pass start; reserved-low-id allocation pinned; ~50 LOC of typecheck IO hard-wire deleted; `lower_perform` synchronous shortcut deleted; renamed `lower_perform_non_io_to_value` → `lower_perform_to_value`; 4 codegen IO-special-case sites' `if p.effect == "IO"` branches dropped; `unsupported_handle_construct` comment cleanup; `main` shim adds top-level IO handler frame push/pop with Phase-4f-style discipline check (debug-only); new runtime fn `sigil_io_println_arm`; `std/io.sigil` comment header rewritten to clarify it's documentation only; 3 new typecheck unit tests + 1 updated.
    - **CI fix #1** (`29b2b5e`) — `stackmap_section_parses_v0_placeholder` count updated 4 → 9 (Slice 1's main shim added 5 net call sites: `handler_frame_new` + `set_arm` + `handle_push` + `handle_pop` + `sigil_perform` replacing direct `sigil_println`; the perform → run_loop path replaced 1 site with 2). Comment enumerates the 9 sites.
    - **Slice 1 review fixups** (`2e12eaa`) — addresses 4 items from the Slice 1 mid-flight reviews: `[DEVIATION Task 57] IO color filter retention` deviation entry corrects the "without correctness loss" framing to "perf-preserving with documented residual gap" (review-1 substantive + review-2 issue #1); `#[ignore]`'d e2e `user_discard_k_io_handler_does_not_unwind_native_color_helper_pending_color_filter_lift` pins the residual gap (review-1 path 1); 3 codegen classifier `"IO"` literals replaced with `crate::color::NATIVE_EFFECT` reference (review-2 issue #3); `[DEVIATION Task 57] Slice 2 shim-snapshot rename` deviation entry pre-flags the Slice 2 rename for review (review-2 issue #4); `debug_assert!(!heap_ptr.is_null())` in `sigil_io_println_arm` (review-2 issue #6).
    - **Slice 2: ArithError refactor** (`28721af`) — `BinOp::SdivUnchecked` / `SremUnchecked` AST variants (codegen-internal; produced only by elaborate); `register_effect_use` typecheck helper unifies row introduction across `check_perform` and `check_binop`; `Expr::Binary { op: Div | Mod, .. }` rewrites at elaborate time to `if rhs == 0 { perform ArithError.{div,mod}_by_zero() } else { Sdiv/SremUnchecked }`; codegen `BinOp::Div`/`Mod` arms become `unreachable!()` post-elaborate; `trap_on_zero` + `TRAP_ARITH_ABORT` deleted; `panic_arith_ref` / `div_zero_gv` / `mod_zero_gv` Lowerer fields under `#[allow(dead_code)]` (matches Slice 1 `println_ref` pattern); two new runtime arm fns `sigil_arith_error_{div,mod}_by_zero_arm` (Plan A2 banner verbatim, exit 2); `sigil_panic_arith_error` deleted from `runtime/src/arith.rs`; `main` shim extended with ArithError frame pushed first (2 arms) + IO frame second (LIFO), discipline-check snapshot moved from `io_frame_ptr` to `arith_frame_ptr` per pre-flag entry; `fn main`'s effect-row constraint expanded from `{IO}` to `{IO, ArithError}` (E0041 message updated); 3 source-file row updates (`examples/arith.sigil`, `examples/div_by_zero.sigil`, `compiler/tests/e2e.rs:446-448`); new `examples/div_recover.sigil` demonstrates algebraic recovery; new e2e `div_recover_example_returns_999` pins the recovery surface; `stackmap_section_parses_v0_placeholder` count updated 9 → 14 (Slice 2 added 5 net shim calls: ArithError frame_new + 2 set_arm + push + pop).
    - **Closeout (this commit)** — README "Verification limits" gains a Plan B Task 57 row covering both refactors with a comprehensive summary; PROGRESS Task 57 entry filled with implementing-commit list (`done-pending-ci`); deviation entry implementing-commit lines back-filled.
  - **Plan A1 IO + Plan A2 arith-panic surfaces retired:** Post-Task-57, `IO` and `ArithError` are both registry-driven effects routing through `sigil_perform`. The `main` shim installs top-level handler frames for both. Default arm fns preserve Plan A2 user-visible behavior verbatim (same stderr banner per operator for div-by-zero / mod-by-zero, same exit code 2). User programs can now wrap arithmetic in `handle ... with { ArithError.div_by_zero(k) => ... }` to recover algebraically — see `examples/div_recover.sigil`. Programs unchanged at the source level for the print-and-divide canonical use case; programs declaring `![]` and doing division/modulo now declare `![ArithError]` explicitly.
  - **Slice 1 IO color filter retention residual gap** (per `[DEVIATION Task 57] IO color filter retention`): the colorer's `NATIVE_EFFECT = "IO"` constant and three parallel codegen classifier filters are kept as a perf-preserving choice. User-installed discard-`k` IO handlers do not unwind Native-color helper fns under this retention. Pinned by `#[ignore]`'d e2e `user_discard_k_io_handler_does_not_unwind_native_color_helper_pending_color_filter_lift`. v2 path: lift the constant + drop the three filters; un-ignore the test. Out of scope for Task 57.
  - **Task 57 deferred items** (separate post-Task-57 chores):
    - Drop the `Lowerer.println_ref` field + `Lowerer.panic_arith_ref` / `div_zero_gv` / `mod_zero_gv` plumbing entirely (16-site destructure refactor; under `#[allow(dead_code)]` today).
    - Delete the codegen-side `sigil_println` and `sigil_panic_arith_error` FFI declarations that no longer have callers (saves a few entries in the per-fn `dfg.ext_funcs` table; structural-only).
    - The `0x40` trap slot reserved by retiring `TRAP_ARITH_ABORT` is available for a future trap-catalogue rework.
- Task 58 — Multi-shot rigor: `examples/choose_demo.sigil`, `examples/multishot_stress.sigil`
  - status: done (squash-merged at `c1d338c` on 2026-04-28 as PR #31)
  - commits: [1fadef4, 3219092, 3f872c5, 312338f, d60152b, 44fd945, 9995c51, squash → c1d338c]
  - notes: PR `plan-b-task-58` off `main` at `7870eea` per per-phase-PR cadence convention. **7 commits** squash-merged:
    - **Foundation** (`1fadef4`) — Task 57 squash-hash flip (`done-pending-ci` → `done` at `7870eea` on 2026-04-28) + `[DEVIATION Task 58]` entry documenting the v1 stress-shape choice. No source code changes.
    - **`examples/choose_demo.sigil`** (`3219092`) — first Sigil program to exercise multi-shot dispatch with an `(Int) -> Int` op signature; existing Slice C e2e tests use `() -> Bool` so do not exercise Phase 4b's perform-side args-buffer packing + Phase 4c's arm-side arg unpacking under the multi-shot trampoline chain. Helper `helper(seed: Int) -> Int ![Choose, IO]` body is the `LetBindThenTail` shape (`let x: Int = perform Choose.choose(seed); x`); arm body is the canonical 2-let multi-shot shape recognised by `arm_body_multi_let_then_pure_tail_shape`. Trace: `helper(5)` performs `Choose.choose(5)`; arm receives `arg=5`; `k(arg + 10) = k(15)` drives helper synth-cont with `x=15` → 15 (r1); `k(arg + 20) = k(25)` drives the SAME heap-reified k_closure a second time with `x=25` → 25 (r2); arm returns r1 + r2 = 40. New e2e `choose_demo_example_returns_40` pins stdout = "40\n", stderr = "", exit 0.
    - **`examples/multishot_stress.sigil`** (`3f872c5`) — five sequential `handle` expressions × 2 resumes per arm = **10 multi-shot k invocations across 5 fresh handler frames and 5 fresh heap-reified k_closure records**, with 10 distinct `(Int)` inputs. The v1 stress shape exercises a complementary invariant to the literal "10+ in single arm" Task 58 wording: multiple FRESH handler frames each driving a fresh k_closure twice, asserting independence across handles (frame allocator + GC bitmap discipline + push/pop sequencing across 5 handle sites in one program). Closed-form expected output: `helper(i)` performs `Choose.choose(i)`; arm: `r1 = k(i+100)`, `r2 = k(i+200)`; arm returns `2*i + 300`. For i ∈ {1,2,3,4,5} → h_i = 302, 304, 306, 308, 310. Total = 1530. The 10 distinct inputs to k across the program (101, 102, 103, 104, 105, 201, 202, 203, 204, 205) ensure that any cross-handle leak (e.g., a stale closure capture from a prior handle's k_closure record) would diverge the closed-form sum from 1530. New e2e `multishot_stress_example_returns_1530` pins stdout = "1530\n", stderr = "", exit 0.
    - **Closeout (this commit)** — PROGRESS Task 58 entry filled with implementing-commit list (`done-pending-ci`); deviation entry's "Implementing commit(s)" line backfilled with the foundation/example/closeout SHAs.
  - **Coverage matrix** — Task 58 v1 ships:
    | Property | choose_demo | multishot_stress |
    |---|---|---|
    | Multi-shot k invocations | 2 | 10 |
    | Distinct handler frames pushed/popped | 1 | 5 |
    | Distinct heap-reified k_closure records | 1 | 5 |
    | Distinct k input values | 2 (15, 25) | 10 (101..105 + 201..205) |
    | Op signature | `(Int) -> Int` | `(Int) -> Int` |
    | Closed-form independence assertion | r1 + r2 = 40 | Σ h_i = 1530 |
    | First-time multi-shot coverage of `(Int) -> Int` | ✓ | ✓ |
    | Cross-handle k_closure independence | — | ✓ |
    | Sequential push/pop discipline across N handles | — | ✓ (N=5) |
  - **The literal Task 58 stress shape** ("10+ resumes within a single arm with different inputs") requires the Slice C N-chain extension explicitly deferred from PR #27 (per `[DEVIATION Task 55] Phase 4e captures+` line 1503's "Deferred from Slice C v1" list and pinned by the e2e test `slice_c_multi_let_arm_body_with_three_lets_is_rejected_at_codegen`). Task 58 v1's ship covers the *correctness rigor* dimension; **Task 60's perf floor** (Multi-shot stress test, 3-element Choose combinator, N=1000 iterations, <5s on both hosts) will exercise the literal-N-resume stress at much higher N via the canonical helper-recursion idiom from `[DEVIATION Task 55] Phase 4e captures+` line 1507's "Stage 9 P20 readiness analysis". The Slice C N-chain extension is **post-Plan-B work** (Plan-C-or-later territory) — not on Plan B's task list, doesn't gate any Stage 6 review checkpoint item, and falls under Plan B's "do not implement Stage 7+ features" guardrail. When/if a future PR ships it, it'll either upgrade `examples/multishot_stress.sigil` or ship a parallel `multishot_stress_n.sigil` demonstrating 10+ resumes in a single arm.
- Task 59 — Examples: `catch.sigil`, `state.sigil`, `choose.sigil` + e2e
  - status: done (squash-merged at `d2dce1f` on 2026-04-28 as PR #32)
  - commits: [f8d4e5a, 54f611a, e892e66, 53e7a7a, 3c85938, 07152ed, squash → d2dce1f]
  - notes: PR `plan-b-task-59` off `main` at `c1d338c` per per-phase-PR cadence convention. **5 commits** on the branch:
    - **Foundation** (`f8d4e5a`) — Task 58 squash-hash flip (`done-pending-ci` → `done` at `c1d338c` on 2026-04-28) + `[DEVIATION Task 59]` entry documenting the v1 expressibility envelope for the three demos. No source code changes.
    - **`examples/catch.sigil`** (`54f611a`) — `effect Raise { fail: () -> Int }` with helper performing `Raise.fail()` in a captures-bearing `LetBindThenTail` body (`let result: Int = perform Raise.fail(); result + input`); main wraps `risky(7)` in `handle ... with { Raise.fail(k) => 42 }`. The discard-`k` arm short-circuits helper's tail (Phase 4e captures+ colorer-handler-discharge refinement); the arm value `42` flows directly to the handle's binding instead of `result + input = 49` which the use-`k` path would have produced. Same correctness invariant as `captures_bearing_synth_cont_arity_n_helper_discard_k`. New e2e `catch_example_recovers_with_42` pins stdout = "42\n", stderr = "", exit 0.
    - **`examples/state.sigil`** (`e892e66`) — `effect State { get: () -> Int, set: (Int) -> Int }` with two sequential `handle` expressions, one per State op. `read_value()` performs `State.get()` in a `LetBindThenTail` body; main's `State.get(k) => k(5)` arm resumes with 5 → count + 1 = 6. `write_value()` performs `State.set(99)`; main's `State.set(arg, k) => k(arg)` arm echoes 99. (Helpers named `_value` rather than `_counter` to avoid suggesting cross-call mutable state — each handle is independent in v1.) Both handles register BOTH op arms per the Phase 4f latent op_id/arm_count constraint (the `examples/div_recover.sigil` precedent for multi-op effect handlers). New e2e `state_example_dual_handle_returns_6_then_99` pins stdout = "6\n99\n", stderr = "", exit 0.
    - **`examples/choose.sigil`** (`53e7a7a`) — `effect Choose resumes: many { flip: () -> Bool }` with helper performing `Choose.flip()` in a `LetBindThenTail` body (`let b: Bool = perform Choose.flip(); if b { 1 } else { 2 }`); main's arm body is the canonical Slice C v1 2-resume shape `{ let r1: Int = k(true); let r2: Int = k(false); r1 + r2 }`. Same shape as `slice_c_choose_multi_shot_arm_invokes_k_twice_with_different_args` with a different helper-tail value (1 vs. 0 for `else`). New e2e `choose_example_dual_resume_returns_3` pins stdout = "3\n", stderr = "", exit 0.
    - **Closeout (this commit)** — PROGRESS Task 59 entry filled with implementing-commit list (`done-pending-ci`); `[DEVIATION Task 59]` implementing-commit line backfilled with the foundation/example/closeout SHAs.
  - **Coverage matrix** — Task 59 ships:
    | Property | catch | state | choose |
    |---|---|---|---|
    | Effect | `Raise` (1 op) | `State` (2 ops: get, set) | `Choose` (1 op, multi-shot) |
    | Helper body shape | LetBindThenTail (captures-bearing) | LetBindThenTail × 2 helpers (captures-free) | LetBindThenTail (captures-free, Bool) |
    | Arm semantics | discard-`k` (recovery) | use-`k` tail-position (echo / fixed seed) | 2-resume multi-shot (k(true), k(false)) |
    | Phase 4e correctness gate exercised | discard-`k` cross-call (PR #26) | use-`k` arm × 2 ops × 2 handles | multi-shot k via Slice C v1 2-let arm (PR #27) |
    | Closed-form expected output | "42\n" | "6\n99\n" | "3\n" |
  - **The literal-plan-wording deltas** are all documented in `[DEVIATION Task 59]`: (a) `state.sigil`'s `run_state` higher-order helper deferred (TypeExpr::Fn + arm-body lambdas, both Plan-C-or-later); (b) `choose.sigil`'s literal "two-flip pair generator" deferred (chained-synth-cont extension for multi-perform helper bodies + Slice C N-chain extension, both Plan-C-or-later). Task 59's demonstration purpose — exercising one-shot Raise recovery, State get/set dispatch, multi-shot Choose dual-resume — ships within the v1 expressibility envelope. The literal interpretations bundle naturally with the post-Plan-B widenings named in `[DEVIATION Task 58]`.
- Task 60 — Performance floor: native fib(20) <50ms; CPS-forced fib(20) <500ms; arena escape ≤1%
  - status: done (squash-merged at `8274225` on 2026-04-28 as PR #33)
  - commits: [e492ede, 02389f5, f510752, 044aa0e, 8516abe, 5d940f3, squash → 8274225]
  - notes: PR `plan-b-task-60` off `main` at `d2dce1f` per per-phase-PR cadence convention. **5 commits** on the branch:
    - **Foundation** (`e492ede`) — Task 59 squash-hash flip (`done-pending-ci` → `done` at `d2dce1f` on 2026-04-28) + `[DEVIATION Task 60]` entry documenting the v1 expressibility envelope for the perf-floor work (three deltas: `State[Int]` → `State` + per-call perform necessary for CPS-coloring; literal "3-element Choose combinator" → single-binary-perform N=1000 driver per chained-synth-cont deferral; "1% NextStep escape rate" → defines "typical CPS-color run" as multishot_perf.sigil + integer-arithmetic ratio assertion). No source code changes.
    - **`examples/fib_cps_perf.sigil`** (`02389f5`) — `effect State { get: () -> Int, set: (Int) -> Int }` + `fn fib(n: Int) -> Int ![State, IO]` with body `let _: Int = perform State.get(); match n { 0 => 0, 1 => 1, _ => fib(n - 1) + fib(n - 2) }`. Each fib invocation performs `State.get()` once at entry, tripping the CPS colorer. Body shape doesn't match `is_simple_let_yield_then_pure_tail_body` (match's tail contains non-pure recursive `fib(...)` calls), so fib is `Color::Cps` but `UserFnAbi::Sync` — synchronous run_loop driver at every perform site. ~17710 synchronous handler dispatches dominate the wall-clock for fib(20) = 6765. Both `get` and `set` arms registered per the Phase 4f latent op_id/arm_count constraint. New e2e `fib_cps_perf_example_prints_6765_under_500ms` pins stdout = "6765\n", stderr = "", exit 0, wall-clock < 500ms (10× the native 50ms floor — the trampolined-arithmetic ceiling).
    - **`examples/multishot_perf.sigil`** (`f510752`) — Recursive driver `fn run(n: Int) -> Int ![IO]` chaining 1000 iterations of the canonical Slice C v1 multi-shot pattern (single-binary-perform helper `outcome()` + 2-resume arm `{ let r1 = k(true); let r2 = k(false); r1 + r2 }`). Total: 2000 multi-shot k invocations across 1000 fresh handler frames + 1000 fresh heap-reified k_closure records. Exercises iteration-scale multi-shot scalability — push/pop discipline + GC bitmap discipline + recursive-driver stack depth. Sentinel print: stdout = "0\n" (run(1000) returns 0; per-iteration values discarded via `let _`). New e2e `multishot_perf_example_under_5s` pins stdout = "0\n", stderr = "", exit 0, wall-clock < 5s.
    - **Arena escape rate e2e** (`044aa0e`, tightened in fixup commit) — new e2e test `arena_escape_count_is_zero_below_one_percent_ceiling` compiles `examples/multishot_perf.sigil` (the representative CPS-color program), runs with `SIGIL_PRINT_STATS=1` env var to trigger the runtime's atexit counter dump, parses `SIGIL_COUNTER_ARENA_ALLOC_COUNT` and `SIGIL_COUNTER_ARENA_ESCAPE_COUNT` from stderr, asserts `escape == 0` (**tighter than the plan's 1% ceiling — matches actual current state**) AND `alloc > 0` (sanity guard against a 0/0 ratio that would trivially pass any escape-rate assertion). Today's v1 codegen has zero compiler-side `sigil_arena_promote` call sites (multi-shot k_closure records are heap-allocated directly via `sigil_alloc` rather than arena-allocated-then-promoted), so the actual escape rate is 0%. The plan's 1% remains the contractual worst-acceptable; the test enforces the tighter actual bound so that any regression — even sub-1% — surfaces immediately and triggers explicit review of whether the change is deliberate.
    - **Closeout (this commit)** — PROGRESS Task 60 entry filled with implementing-commit list (`done-pending-ci`); `[DEVIATION Task 60]` "Implementing commit(s)" line backfilled with concrete SHAs.
  - **Coverage matrix** — Task 60 ships:
    | Floor | Target | Example | Asserted by |
    |---|---|---|---|
    | Native fib(20) | <50ms | `fib_perf.sigil` (Plan A2 pre-existing) | `fib_perf_example_prints_6765_under_50ms` |
    | CPS-forced fib(20) | <500ms (10× ceiling) | `fib_cps_perf.sigil` (new) | `fib_cps_perf_example_prints_6765_under_500ms` |
    | Multi-shot stress | <5s (1000 iter × 2 resumes) | `multishot_perf.sigil` (new) | `multishot_perf_example_under_5s` |
    | Arena escape rate | =0 (tighter than plan's ≤1% ceiling — matches actual state) | `multishot_perf.sigil` (reused) | `arena_escape_count_is_zero_below_one_percent_ceiling` |
  - **Three literal-plan-wording deltas** documented in `[DEVIATION Task 60]`: (a) `State[Int]` → `State` (Sigil v1 monomorphic; type-parameter granularity doesn't change CPS-coloring); (b) literal 3-element Choose combinator → single-binary-perform N=1000 driver (literal Cartesian-product 3-pick requires multi-perform helper bodies, deferred to chained-synth-cont extension; bundles with Slice C N-chain extension named in [DEVIATION Task 58]); (c) "typical CPS-color run" → defined as multishot_perf.sigil for the escape-rate assertion. None of the three deltas requires new compiler machinery.
- Task 61 — Validation prompts P18, P19, P20 (prompt bank complete)
  - status: in-progress
  - commits: [HEAD]
  - notes: PR `plan-b-task-61` off `main` at `8274225` per per-phase-PR cadence convention. Foundation commit (this commit) lands Task 60 squash-hash flip (`done-pending-ci` → `done` at `8274225` on 2026-04-28) + `[DEVIATION Task 61]` entry documenting the v1 expressibility envelope for the three new prompts (P18 Raise[String]-based safe parser ships graded end-to-end; P19 State[Int] counter via list walk graded against "compiles" until `run_state` higher-order lift ships; P20 multi-shot Choose all-pairs graded against "compiles" until chained-synth-cont extension ships per [DEVIATION Task 60]). No source code changes in foundation. Subsequent commit appends P18 / P19 / P20 to `spec/validation-prompts.md` (the bank reaches its 20-entry target). Closeout updates this entry to `done-pending-ci`.

### Stage 6 review checkpoint

Pending — request human review of linearity, handler stack semantics, multi-shot correctness, IO refactor, arena correctness, color decisions before declaring Plan B complete.
