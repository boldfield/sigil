/target/
**/*.rs.bk
.DS_Store
__pycache__/
*.pyc

# Validation harness per-run JSONL traces (the markdown report at
# spec/validation-log.md is the checked-in artifact; per-run traces
# are local-only).
spec/validation-results-*.jsonl

# Cross-language comparison harness — per-run JSONL traces are
# local-only; the markdown report at comp/log/comparison-log.md is
# the checked-in summary.
comp/log/comparison-results-*.jsonl
